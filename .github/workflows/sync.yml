on:
  schedule:
  - cron: '0 18 * * *'
  workflow_dispatch: 

jobs:
  repo-sync:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        persist-credentials: false
    - name: repo-sync
      uses: repo-sync/github-sync@v2
      with:
        source_repo: "www.github.com/givinkwon/boltnnut-platform-front"
        source_branch: "master"
        destination_branch: "www.github.com/givinkwon/boltnnut-platform-front-test"
        github_token: ${{ secrets.sync || github.token }}
